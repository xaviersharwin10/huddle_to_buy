# Build artifacts
node_modules/
dist/
build/
.next/

# Local-only configs and secrets
.env
.env.*
!.env.*.example

# Logs
logs/

# AXL — upstream source not needed; binary (axl/bin/node) is a committed
# x86-64 Linux ELF and is copied directly — no Go build required
axl/upstream/

# Contract build outputs
contracts/artifacts/
contracts/cache/
contracts/typechain-types/

# Git history (large, not needed in build context)
.git/

# Claude Code internals
.claude/
